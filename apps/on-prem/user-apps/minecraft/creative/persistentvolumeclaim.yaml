apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-bedrock-creative-data
  namespace: minecraft
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
