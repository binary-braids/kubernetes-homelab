apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: telegraf-snmp-data-pvc
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi