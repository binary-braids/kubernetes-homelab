apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prod-cert
  namespace: traefik
spec:
  secretName: prod-cert-secret
  dnsNames:
    - '*.binarybraids.com'
    - 'binarybraids.com'
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer