apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: phpipam-data-pvc
  namespace: phpipam
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1.5Gi
