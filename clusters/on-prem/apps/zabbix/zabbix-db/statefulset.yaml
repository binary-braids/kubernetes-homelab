apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zabbix-mariadb
  namespace: zabbix
  labels:
    app: zabbix-mariadb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zabbix-mariadb
  serviceName: zabbix-mariadb
  template:
    metadata:
      labels:
        app: zabbix-mariadb
    spec:
      containers:
      - name: zabbix-mariadb
        image: mariadb:11.2.4-jammy@sha256:7830bb0df5a8eff943f32eb5455924570a23c7083e2d21a16f0377a5e50a8a4d
        args:
          - mariadbd          
          - --character-set-server=utf8
          - --collation-server=utf8_bin 
          - --default-authentication-plugin=mysql_native_password        
        ports:
        - containerPort: 5432
        env:
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: zabbix-secret
              key: zabbix_mariadb_user
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: zabbix-secret
              key: zabbix_mariadb_password
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: zabbix-secret
              key: zabbix_mariadb_root_password
        - name: MYSQL_DATABASE
          value: zabbix
        volumeMounts:
          - name: mariadb-volume
            mountPath: "/var/lib/mysql"
      volumes:
      - name: mariadb-volume
        persistentVolumeClaim:
          claimName: zabbix-mariadb-pvc
