apiVersion: v1
kind: Service
metadata:
  name: mealie-service
  namespace: mealie
spec:
  selector:
    app: mealie
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 80