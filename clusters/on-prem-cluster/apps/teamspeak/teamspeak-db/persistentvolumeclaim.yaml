apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teamspeak-db-pvc
  namespace: teamspeak
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200M