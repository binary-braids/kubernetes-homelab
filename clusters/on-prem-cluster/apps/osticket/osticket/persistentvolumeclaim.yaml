apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: osticket-pvc
  namespace: osticket
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 256Mi
