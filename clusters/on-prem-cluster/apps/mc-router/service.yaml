apiVersion: v1
kind: Service
metadata:
  name: mc-router
  namespace: mc-router
spec:
  type: LoadBalancer
  ports:
  - targetPort: proxy
    name: proxy
    port: 25565
  selector:
    run: mc-router