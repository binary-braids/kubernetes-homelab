apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filezilla-data-pvc
  namespace: filezilla
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi
