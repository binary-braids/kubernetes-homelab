apiVersion: v1
kind: ConfigMap
metadata:
  name: values
  namespace: monitoring
data:
  values.yaml: |-
    prometheus:
      prometheusSpec:
        storageSpec:
          volumeClaimTemplate:
            spec:
              storageClassName: longhorn
              resources:
                requests:
                  storage: 1Gi