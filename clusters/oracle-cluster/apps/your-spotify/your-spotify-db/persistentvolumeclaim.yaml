apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: your-spotify-db-pvc
  namespace: your-spotify
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi
