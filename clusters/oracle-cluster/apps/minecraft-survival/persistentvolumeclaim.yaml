apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-survival-pvc
  namespace: minecraft-survival
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi