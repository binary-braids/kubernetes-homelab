apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: osticket-mariadb
  namespace: osticket
  labels:
    app: osticket-mariadb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: osticket-mariadb
  serviceName: osticket-mariadb
  template:
    metadata:
      labels:
        app: osticket-mariadb
    spec:
      containers:
      - name: osticket-mariadb
        image: mariadb:lts@sha256:5dfb3093333fa0ea53194ddef0a2bfa21d3b1e1353bd228b22610cd6fc0c04da
        imagePullPolicy: Always
        ports:
        - containerPort: 3306
          protocol: TCP
        env:
        - name: MARIADB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: osticket-secret
              key: mariadb_root_password
        - name: MARIADB_DATABASE
          value: "osticket"
        - name: MARIADB_USER
          value: "osticket"
        - name: MARIADB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: osticket-secret
              key: osticket_db_password
        volumeMounts:
        - mountPath: /var/lib/mysql
          name: osticket-mariadb-volume
      volumes:
      - name: osticket-mariadb-volume
        persistentVolumeClaim:
          claimName: osticket-data-pvc
