apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: osticket-data-pvc
  namespace: osticket
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 512Mi
