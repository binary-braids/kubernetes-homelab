apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emulatorjs-pvc
  namespace: emulatorjs
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi